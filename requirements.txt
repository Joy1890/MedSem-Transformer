# Sent-e-Med Requirements
# Install with: pip install -r requirements.txt

# Core deep learning
torch>=2.0.0
torchvision>=0.15.0   # optional, may be needed depending on torch install

# Sentence embeddings (SBERT)
sentence-transformers>=2.2.0

# Data processing
pandas>=1.5.0
numpy>=1.23.0

# Evaluation metrics
scikit-learn>=1.2.0

# Progress bars
tqdm>=4.65.0

# Optional: for faster tokenization in sentence-transformers
# tokenizers>=0.13.0
